const fs=require('fs'); const assert=require('assert');
const app=fs.readFileSync('src/public/PublicApp.tsx','utf8');
const css=fs.readFileSync('src/public/public.css','utf8');
const main=fs.readFileSync('src/public-main.tsx','utf8');
for(const route of ['/community','/studio','/professional','/consultant','/assessments','/pricing','/trust','/downloads','/knowledge','/support-development','/legal']) assert(app.includes(route),`missing route ${route}`);
for(const text of ['Turn raw Windows audit evidence into','Run TAS Community','TAS Professional','TAS Consultant','Browser-local Community','Payment integration is intentionally deferred']) assert(app.includes(text),`missing public content: ${text}`);
assert(app.includes("if(path==='/studio')"),'Studio routing missing');
assert(app.includes('<CommunityApp/>'),'Community Studio is not integrated');
assert(!app.includes('fetch(') && !app.includes('XMLHttpRequest'),'Public/Community shell must not upload evidence');
assert(main.includes("./public/PublicApp"),'Public shell is not the main build');
assert(css.includes('@media(max-width:900px)') && css.includes('@media(max-width:560px)'),'Responsive gates missing');
assert(fs.existsSync('src/App.tsx') && fs.existsSync('src/styles.css'),'Protected AURORA source missing');
console.log('[PASS] TAS v70.21 public routes, privacy boundary, responsive shell and Community Studio integration verified.');
